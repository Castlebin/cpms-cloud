<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cpms.system.modules.sys.mapper.SysTopMenuMapper">

    <select id="queryRoleTopMenus" resultType="com.cpms.system.modules.sys.entity.SysTopMenuEntity">
        select *
        from cpms_system_top_menu
        where del_flag = 0
        and top_menu_id IN
        ( SELECT menu_id FROM cpms_system_role_menu WHERE type = 1 and  role_id IN
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        )
    </select>

</mapper>