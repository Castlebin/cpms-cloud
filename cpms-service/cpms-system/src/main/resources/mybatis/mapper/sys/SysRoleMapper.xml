<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cpms.system.modouls.sys.mapper.SysRoleMapper">

    <select id="queryRoleByUserId" parameterType="java.lang.String" resultType="com.cpms.system.modouls.sys.entity.SysRoleEntity">
        SELECT r.role_name,r.role_id,role_desc,ru.user_id
        FROM  cpms_system_role r,cpms_system_role_user ru
        WHERE ru.role_id= r.role_id AND r.del_flag=0 AND ru.user_id = #{userId};
    </select>

</mapper>